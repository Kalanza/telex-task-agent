# Task Reminder Agent - Environment Variables Example
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# ============================================
# Telex Integration (Optional)
# ============================================
# Webhook URL for sending reminders to Telex platform
# Leave empty to disable external notifications (WebSocket only)
TELEX_WEBHOOK_URL=

# ============================================
# Server Configuration (Optional)
# ============================================
# Port number for the server (default: 9000)
PORT=9000

# Host address (default: 0.0.0.0 for all interfaces)
HOST=0.0.0.0

# ============================================
# Database Configuration (Optional)
# ============================================
# Path to SQLite database file (default: db/tasks.db)
DATABASE_PATH=db/tasks.db

# ============================================
# Scheduler Configuration (Optional)
# ============================================
# Reminder check interval in seconds (default: 30)
# Minimum recommended: 10 seconds
REMINDER_CHECK_INTERVAL=30

# ============================================
# Logging Configuration (Optional)
# ============================================
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# Log file path (default: logs/app.log)
LOG_FILE=logs/app.log

# ============================================
# Security Configuration (Optional)
# ============================================
# Set to 'true' in production for enhanced security
PRODUCTION_MODE=false

# Allowed origins for CORS (comma-separated)
# Default: * (all origins) - restrict in production!
ALLOWED_ORIGINS=*

# ============================================
# Notes
# ============================================
# - All variables are optional and have sensible defaults
# - Database and logs directories are created automatically
# - In production, set PRODUCTION_MODE=true and restrict ALLOWED_ORIGINS
# - The .env file is automatically ignored by git
